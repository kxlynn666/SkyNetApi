# Store/theme checks

Os checks desta branch validam sintaxe, catálogo de decorações de nome, contrato cliente/CSS, cobertura estrutural do tema, filtros `hidden` e permanência do observador de estado da loja.
